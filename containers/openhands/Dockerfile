FROM python:3.12-slim-bookworm@sha256:782412e85d0f0984994c290652577d4018aff08145c85b262bb63dc0c7522254
RUN apt-get update && apt-get install -y --no-install-recommends tmux git ripgrep chromium curl ca-certificates && rm -rf /var/lib/apt/lists/*
COPY openhands-requirements.txt /opt/requirements.txt
COPY openhands-linux.lock /opt/openhands-linux.lock
RUN pip install --no-cache-dir uv==0.12.3 && uv pip install --system --no-cache -r /opt/openhands-linux.lock
ENV OPENHANDS_SUPPRESS_BANNER=1 ANONYMIZED_TELEMETRY=false DO_NOT_TRACK=1 PYTHONPATH=/opt/runtime
WORKDIR /workspace/candidate
CMD ["sleep", "infinity"]
